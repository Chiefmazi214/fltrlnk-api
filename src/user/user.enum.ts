// user status
export enum UserStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  BANNED = 'banned',
  INACTIVE = 'inactive',
}

// profile type
export enum ProfileType {
  BUSINESS = 'business',
  INDIVIDUAL = 'individual',
  USER = 'user',
}
