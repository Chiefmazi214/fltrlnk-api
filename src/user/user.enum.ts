// user status
export enum UserStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  BANNED = 'banned',
  INACTIVE = 'inactive',
}

// profile type
export enum ProfileType {
  BUSINESS = 'business',
  INDIVIDUAL = 'individual',
}

export enum UserTier {
  FREE = 'free',
  BASIC = 'basic',
  PRO = 'pro',
}