export enum LikeType {
  PROFILE = 'profile',
  POST = 'post',
}

export enum FollowStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}

export enum FollowStatusFilter {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  ALL = 'all',
}